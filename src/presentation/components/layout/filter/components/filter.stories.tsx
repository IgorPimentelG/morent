import { Meta, StoryObj } from '@storybook/react';
import { Filter } from '@presentation/components/layout';

export default {
	title: 'Components/Layout/Filter',
	component: Filter
} as Meta;

export const Default: StoryObj = {};
