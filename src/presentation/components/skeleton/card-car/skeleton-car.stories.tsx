import { Meta, StoryObj } from '@storybook/react';
import { SkeletonCar } from '@presentation/components/skeleton';

export default {
	title: 'Components/Skeletons/card-car',
	component: SkeletonCar
} as Meta;

export const Default: StoryObj = {};
